apiVersion: v1
kind: Service
metadata:
  labels:
    app: email-microservice
  name: email-microservice
  namespace: development
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: email-microservice
  type: ClusterIP