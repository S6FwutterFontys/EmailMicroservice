apiVersion: v1
kind: Service
metadata:
  name: email-microservice
  namespace: development
spec:
  selector:
    app: email-microservice
    type: ClusterIP
  ports:
    - port: 80
      targetPort: 80