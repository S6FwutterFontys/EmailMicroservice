apiVersion: v1
kind: Service
metadata:
  labels:
    app: email-microservice
  name: email-microservice
  namespace: development
spec:
  selector:
    app: email-microservice
    type: ClusterIP
  ports:
    - port: 80
      targetPort: 80